LISTEN_HOST=localhost
LISTEN_PORT=8000
DATABASE_PATH=var/database/world-flipper.db
CDN_DIR=.cdn
ASSET_MANIFEST_DIR=content/asset-lists
MASTER_DATA_DIR=content/master
LIVE_CONTENT_DIR=content/live
# Optional: override season.json without editing tracked content.
SEASON_START_AT=2026-09-05T00:00:00+07:00

# Self-service player-save import is destructive and disabled by default.
# Export remains available to the authenticated viewer. Enable import only on a trusted/private server.
PLAYER_DATA_IMPORT_ENABLED=false
PAYMENT_FREE_PACKS_ENABLED=false
